body
    h1 MannCo. Inventory List
    #result-set
    #question Question: How many Sandviches does the Heavy have?
    #bonus-question Bonus-Question: What is Mann Co's secret weapon?

    script(type="text/javascript", src="javascripts/jquery.js")
    script(type="text/javascript")
        $(function() {
            var dirToSearch = 'inventory';
            $.post('/list', {dir : dirToSearch}, function(data) {
                $('#result-set').html('<pre>' + data.results + '</pre>');
            });
        });
